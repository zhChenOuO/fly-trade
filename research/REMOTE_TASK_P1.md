# REMOTE_TASK_P1 — 在 GPU 機器上跑 Phase 1a(Level 1-2,真實 FlyWire)
你是 remote codex。先與 origin 同步 main 分支(用系統 git,這台沒有 rtk),再建立新分支 `remote/phase1a`(不要推 main、不要 force push)。
必讀: research/SPEC_v3.md(尤其「Phase 0 判定」與「Phase 1a」兩節,設計與門檻已凍結,**不得更動**)、research/SPEC.md、research/pipeline/{statistics,fly_simulator,torch_sim,retina,flywire_graph,phase0}.py、research/run_experiment.py、research/v2_health.py、research/outputs/v3/{dynamics_remote.json,phase0_report_remote.json}。
任務: 新增 research/run_phase1a.py(不要改動舊的 run_experiment.py),依 SPEC_v3 的 Phase 1a 用 torch backend 產生 A/B/C/E 的 decisions(欄位契約見 SPEC.md:sample_id,seed,group,buy_score,sell_score,action,latency_ms),計算 market_state、nuisance_r2、repeat_actions/repeat_margins、perturbation_margins,呼叫 statistics.evaluate_levels,輸出 research/outputs/v3/phase1a_remote/{metrics.json, sensitivity.json, health.json, run.log};decisions.parquet 已被 gitignore,不要 commit。z-score 基準存 research/outputs/v3/baseline_remote.json。
正確性要求: (1) 開跑前先用 20 張 Val 圖確認 BUY 比例不塌縮(minority>=0.05)、輸出隨輸入變化,失敗就停下回報;(2) 決策 bar 對齊沿用 run_experiment.get_windows 的規則,並斷言重渲染圖 == images.npy;(3) 不得讀取 label/future_return 來做任何設計決策(evaluate_levels 內部使用是規格允許的);(4) 每個 seed 的 A 決策必須可由固定 seed 重現(記錄 torch/cuda 版本);(5) 記憶體:分批,VRAM <=80%,並定期印進度。
交付: 在 remote/phase1a 分支 commit 並 push 程式與 outputs/v3/phase1a_remote 的小檔;commit 結尾加 `Co-Authored-By: Codex <noreply@openai.com>`。最後回報: 各 Level 1-2 項目數值、耗時、吞吐、任何偏離本文件之處。若任何步驟失敗,停下並如實回報失敗輸出,不要為了通過而改規則。
